import styled from "styled-components";

export const ValidatorMainWrapper = styled.div`
  // padding: 0 ${(props) => props.theme.space(2.5)}
  // ${(props) => props.theme.space(5)};
  // padding: 0 2.5px 5px;
`;

export const ValidatorContentWrapper = styled.div`
  // padding: 0 ${(props) => props.theme.spacing(2.5)};
  // padding: 2.5px;
`;
